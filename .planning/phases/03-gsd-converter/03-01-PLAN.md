---
phase: 03-gsd-converter
plan: 01
type: execute
depends_on: []
files_modified: [core/gsd_converter/__init__.py, core/gsd_converter/parser.py, core/gsd_converter/models.py]
---

<objective>
PLAN.md 파서 구현

Purpose: GSD PLAN.md 파일을 파싱하여 구조화된 PlanModel 객체로 변환하는 기반 모듈 구축
Output: core/gsd_converter/ 패키지 (parser.py, models.py)
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/02-architecture/02-01-SUMMARY.md

**Tech stack available:**
- Python 3.11+
- pyyaml (YAML frontmatter 파싱)
- re (정규식 - XML 태그 파싱)
- dataclasses (데이터 모델)

**Established patterns (from 01-02-SUMMARY):**
- PLAN.md Frontmatter: phase, plan, type, depends_on, files_modified
- XML 구조: objective, context, tasks, verification, success_criteria
- Task elements: name, files, action, verify, done

**Key decisions from Phase 2:**
- GSDConverter 인터페이스: parse_plan(path) → PlanModel
- PlanModel 구조: phase, plan, type, depends_on, files_modified, objective, tasks[]
</context>

<tasks>

<task type="auto">
  <name>Task 1: 데이터 모델 정의</name>
  <files>core/gsd_converter/models.py</files>
  <action>
  PlanModel 및 TaskModel dataclass 정의:

  ```python
  @dataclass
  class TaskModel:
      name: str
      type: str  # "auto" | "checkpoint:human-verify" | "checkpoint:decision"
      files: List[str]
      action: str
      verify: Optional[str]
      done: str

  @dataclass
  class PlanModel:
      phase: str
      plan: int
      type: str  # "execute" | "tdd"
      depends_on: List[str]
      files_modified: List[str]
      objective: str
      context: List[str]
      tasks: List[TaskModel]
      verification: List[str]
      success_criteria: List[str]
  ```

  주의: Optional 필드에 기본값 None 설정. List 필드에 field(default_factory=list) 사용.
  </action>
  <verify>python -c "from core.gsd_converter.models import PlanModel, TaskModel; print('Models OK')"</verify>
  <done>PlanModel, TaskModel 클래스가 임포트되고 인스턴스 생성 가능</done>
</task>

<task type="auto">
  <name>Task 2: Frontmatter 파서 구현</name>
  <files>core/gsd_converter/parser.py</files>
  <action>
  YAML frontmatter를 파싱하는 함수 구현:

  ```python
  def parse_frontmatter(content: str) -> dict:
      """
      PLAN.md의 YAML frontmatter를 파싱.
      --- 로 시작하고 --- 로 끝나는 부분 추출.
      """
  ```

  - pyyaml.safe_load() 사용
  - frontmatter가 없으면 ParseError 발생
  - 필수 필드 검증: phase, plan, type
  </action>
  <verify>python -c "from core.gsd_converter.parser import parse_frontmatter; print(parse_frontmatter('---\nphase: test\nplan: 1\ntype: execute\n---\n'))"</verify>
  <done>frontmatter 파싱 성공, 필수 필드 검증 동작</done>
</task>

<task type="auto">
  <name>Task 3: XML 태그 파서 구현</name>
  <files>core/gsd_converter/parser.py</files>
  <action>
  XML 스타일 태그를 파싱하는 함수들 구현:

  ```python
  def parse_xml_section(content: str, tag: str) -> str:
      """<tag>...</tag> 내용 추출"""

  def parse_tasks(content: str) -> List[TaskModel]:
      """<tasks> 섹션 내 모든 <task> 파싱"""
  ```

  - 정규식 사용: `<{tag}[^>]*>(.*?)</{tag}>` (re.DOTALL)
  - task의 type 속성 추출: `type="..."` 패턴
  - 중첩 태그 처리: name, files, action, verify, done
  </action>
  <verify>python -c "from core.gsd_converter.parser import parse_xml_section; print(parse_xml_section('<test>hello</test>', 'test'))"</verify>
  <done>objective, tasks 등 모든 XML 섹션 파싱 성공</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -c "from core.gsd_converter import models, parser"` 성공
- [ ] PlanModel, TaskModel 인스턴스 생성 가능
- [ ] frontmatter 파싱 동작
- [ ] XML 섹션 파싱 동작
</verification>

<success_criteria>
- 데이터 모델 정의 완료
- Frontmatter 파서 구현
- XML 태그 파서 구현
- 기본 모듈 구조 완성
</success_criteria>

<output>
After completion, create `.planning/phases/03-gsd-converter/03-01-SUMMARY.md`
</output>
