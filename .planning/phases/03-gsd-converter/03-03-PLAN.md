---
phase: 03-gsd-converter
plan: 03
type: execute
depends_on: ["03-02"]
files_modified: [core/gsd_converter/validator.py, core/gsd_converter/errors.py, core/gsd_converter/__init__.py]
---

<objective>
검증 및 에러 핸들링 구현

Purpose: PLAN.md 파싱 및 변환 과정의 오류를 감지하고 사용자에게 명확한 피드백 제공
Output: validator.py (검증 로직), errors.py (커스텀 예외)
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gsd-converter/03-01-SUMMARY.md
@.planning/phases/03-gsd-converter/03-02-SUMMARY.md

**From 03-01, 03-02:**
- PlanModel, TaskModel, ImplementationPlan 클래스 사용 가능
- GSDConverter 클래스 사용 가능

**Validation requirements (from 01-02-SUMMARY):**
- Frontmatter 검증: phase, plan, type 필수, depends_on은 배열
- Task 검증: name, files, action 필수 (auto type)
- 에러 핸들링: 파싱 실패 시 상세 오류 메시지, 부분 성공 지원

**Error types (from 02-02-SUMMARY):**
- ParseError: 형식 오류
- ValidationError: 필수 필드 누락
</context>

<tasks>

<task type="auto">
  <name>Task 1: 커스텀 예외 정의</name>
  <files>core/gsd_converter/errors.py</files>
  <action>
  GSD Converter 전용 예외 클래스 정의:

  ```python
  class GSDConverterError(Exception):
      """Base exception for GSD Converter"""
      pass

  class ParseError(GSDConverterError):
      """PLAN.md 파싱 실패"""
      def __init__(self, message: str, line: int = None, context: str = None):
          self.line = line
          self.context = context
          super().__init__(self._format_message(message))

      def _format_message(self, message: str) -> str:
          parts = [message]
          if self.line:
              parts.append(f"Line: {self.line}")
          if self.context:
              parts.append(f"Context: {self.context}")
          return " | ".join(parts)

  class ValidationError(GSDConverterError):
      """필수 필드 누락 또는 형식 오류"""
      def __init__(self, field: str, expected: str, actual: str = None):
          self.field = field
          self.expected = expected
          self.actual = actual
          message = f"Validation failed for '{field}': expected {expected}"
          if actual:
              message += f", got {actual}"
          super().__init__(message)

  class ConversionError(GSDConverterError):
      """변환 과정 오류"""
      pass
  ```

  주의: 모든 예외는 GSDConverterError를 상속하여 일괄 catch 가능
  </action>
  <verify>python -c "from core.gsd_converter.errors import ParseError, ValidationError; raise ParseError('test', line=10)" 2>&1 | grep -q "Line: 10" && echo "OK"</verify>
  <done>ParseError, ValidationError 예외가 상세 메시지와 함께 동작</done>
</task>

<task type="auto">
  <name>Task 2: 검증 로직 구현</name>
  <files>core/gsd_converter/validator.py</files>
  <action>
  PlanModel 검증기 구현:

  ```python
  class PlanValidator:
      def validate(self, plan: PlanModel) -> List[str]:
          """
          PlanModel 검증. 오류 목록 반환 (빈 리스트 = 유효).
          """
          errors = []
          errors.extend(self._validate_frontmatter(plan))
          errors.extend(self._validate_tasks(plan.tasks))
          return errors

      def _validate_frontmatter(self, plan: PlanModel) -> List[str]:
          """phase, plan, type 필수 검증"""

      def _validate_tasks(self, tasks: List[TaskModel]) -> List[str]:
          """각 task의 필수 필드 검증"""

      def validate_or_raise(self, plan: PlanModel) -> None:
          """검증 실패 시 ValidationError 발생"""
  ```

  검증 규칙:
  - phase: 비어있지 않은 문자열
  - plan: 양의 정수
  - type: "execute" | "tdd"
  - task.name: 비어있지 않은 문자열
  - task.type: 유효한 타입 ("auto", "checkpoint:human-verify", "checkpoint:decision")
  - task.action: 비어있지 않은 문자열 (auto 타입일 경우)
  </action>
  <verify>python -c "from core.gsd_converter.validator import PlanValidator; v = PlanValidator(); print('Validator OK')"</verify>
  <done>PlanValidator.validate() 호출 시 오류 목록 반환</done>
</task>

<task type="auto">
  <name>Task 3: Converter에 검증 통합</name>
  <files>core/gsd_converter/converter.py, core/gsd_converter/__init__.py</files>
  <action>
  GSDConverter에 검증 로직 통합:

  1. converter.py 수정:
  ```python
  class GSDConverter:
      def __init__(self, validate: bool = True):
          self.validate = validate
          self.validator = PlanValidator() if validate else None

      def parse_plan(self, path: str) -> PlanModel:
          # 기존 파싱 로직
          plan = ...
          if self.validator:
              self.validator.validate_or_raise(plan)
          return plan

      def convert(self, plan_path: str, output_path: str = None) -> ImplementationPlan:
          # 기존 변환 로직 + 에러 핸들링
          try:
              plan = self.parse_plan(plan_path)
              impl_plan = self.to_implementation_plan(plan)
              if output_path:
                  with open(output_path, 'w') as f:
                      f.write(impl_plan.to_json())
              return impl_plan
          except GSDConverterError:
              raise
          except Exception as e:
              raise ConversionError(f"Unexpected error: {e}") from e
  ```

  2. __init__.py 업데이트:
  - PlanValidator, 예외 클래스들 export 추가
  </action>
  <verify>python -c "from core.gsd_converter import GSDConverter, PlanValidator, ParseError, ValidationError; print('Full package OK')"</verify>
  <done>GSDConverter가 자동 검증을 수행하고, 오류 시 명확한 예외 발생</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -c "from core.gsd_converter import *"` 성공
- [ ] ParseError, ValidationError가 상세 메시지 포함
- [ ] PlanValidator.validate() 동작
- [ ] GSDConverter가 검증 실패 시 ValidationError 발생
</verification>

<success_criteria>
- 커스텀 예외 클래스 정의 완료
- 검증 로직 구현 완료
- Converter에 검증 통합
- Phase 3 GSD Converter 완료
</success_criteria>

<output>
After completion, create `.planning/phases/03-gsd-converter/03-03-SUMMARY.md`
</output>
