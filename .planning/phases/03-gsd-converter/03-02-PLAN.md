---
phase: 03-gsd-converter
plan: 02
type: execute
depends_on: ["03-01"]
files_modified: [core/gsd_converter/converter.py, core/gsd_converter/schemas.py]
---

<objective>
JSON 스키마 정의 및 변환 로직 구현

Purpose: PlanModel을 Auto-Claude의 implementation_plan.json 형식으로 변환
Output: converter.py (변환 로직), schemas.py (JSON 스키마)
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gsd-converter/03-01-SUMMARY.md

**From 03-01:**
- PlanModel, TaskModel 데이터 클래스 사용 가능
- parse_frontmatter(), parse_xml_section(), parse_tasks() 함수 사용 가능

**Target schema (implementation_plan.json):**
```json
{
  "spec_id": "03-gsd-converter-01",
  "subtasks": [
    {
      "id": "1",
      "title": "Task 1: ...",
      "description": "...",
      "files": ["path/to/file.py"],
      "status": "pending",
      "dependencies": []
    }
  ]
}
```

**Field mapping (from 01-02-SUMMARY):**
- phase + plan → spec_id
- <task> → subtasks[]
- <name> → title
- <action> → description
- type="auto" → status: "pending"
- type="checkpoint:*" → status: "blocked"
</context>

<tasks>

<task type="auto">
  <name>Task 1: JSON 스키마 정의</name>
  <files>core/gsd_converter/schemas.py</files>
  <action>
  implementation_plan.json 스키마를 dataclass로 정의:

  ```python
  @dataclass
  class Subtask:
      id: str
      title: str
      description: str
      files: List[str]
      status: str  # "pending" | "in_progress" | "completed" | "blocked"
      dependencies: List[str]

  @dataclass
  class ImplementationPlan:
      spec_id: str
      subtasks: List[Subtask]

      def to_dict(self) -> dict:
          """JSON 직렬화를 위한 dict 변환"""

      def to_json(self, indent: int = 2) -> str:
          """JSON 문자열로 변환"""
  ```

  주의: dataclasses.asdict() 사용하여 재귀적 dict 변환
  </action>
  <verify>python -c "from core.gsd_converter.schemas import ImplementationPlan, Subtask; p = ImplementationPlan('test', []); print(p.to_json())"</verify>
  <done>ImplementationPlan.to_json() 호출 시 유효한 JSON 출력</done>
</task>

<task type="auto">
  <name>Task 2: 변환 로직 구현</name>
  <files>core/gsd_converter/converter.py</files>
  <action>
  PlanModel → ImplementationPlan 변환기 구현:

  ```python
  class GSDConverter:
      def parse_plan(self, path: str) -> PlanModel:
          """PLAN.md 파일을 읽고 PlanModel로 파싱"""

      def to_implementation_plan(self, plan: PlanModel) -> ImplementationPlan:
          """PlanModel을 ImplementationPlan으로 변환"""

      def convert(self, plan_path: str, output_path: str) -> ImplementationPlan:
          """PLAN.md → implementation_plan.json 전체 변환"""
  ```

  변환 규칙:
  - spec_id = f"{plan.phase}-{plan.plan:02d}"
  - subtask.id = str(index + 1)
  - subtask.title = task.name
  - subtask.description = task.action
  - subtask.status = "pending" if type=="auto" else "blocked"
  - subtask.dependencies = [] (기본값)
  </action>
  <verify>python -c "from core.gsd_converter.converter import GSDConverter; print('Converter OK')"</verify>
  <done>GSDConverter 클래스 임포트 및 인스턴스 생성 가능</done>
</task>

<task type="auto">
  <name>Task 3: 패키지 통합</name>
  <files>core/gsd_converter/__init__.py</files>
  <action>
  패키지 진입점 설정:

  ```python
  from .models import PlanModel, TaskModel
  from .schemas import ImplementationPlan, Subtask
  from .converter import GSDConverter

  __all__ = [
      "PlanModel",
      "TaskModel",
      "ImplementationPlan",
      "Subtask",
      "GSDConverter",
  ]
  ```

  - 모든 공개 API를 __init__.py에서 export
  - __all__ 리스트로 명시적 공개 API 정의
  </action>
  <verify>python -c "from core.gsd_converter import GSDConverter, PlanModel, ImplementationPlan; print('Package OK')"</verify>
  <done>core.gsd_converter 패키지에서 주요 클래스 직접 임포트 가능</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -c "from core.gsd_converter import GSDConverter"` 성공
- [ ] ImplementationPlan.to_json() 동작
- [ ] GSDConverter 클래스 메서드 정의 확인
</verification>

<success_criteria>
- JSON 스키마 dataclass 정의 완료
- GSDConverter 변환 로직 구현
- 패키지 통합 완료
</success_criteria>

<output>
After completion, create `.planning/phases/03-gsd-converter/03-02-SUMMARY.md`
</output>
