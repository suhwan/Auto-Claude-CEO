---
phase: 5
plan: 2
name: Context 저장/로드 구현
estimated_minutes: 15
parallel_safe: true
depends_on: 05-01
---

<objective>
LeaderContext의 파일 시스템 영속화 구현. 프로젝트별로 컨텍스트를 저장하고 로드하는 스토리지 계층.
</objective>

<execution_context>
@apps/backend/extensions/ceo/leader_context/models.py
@apps/backend/extensions/ceo/leader_context/serializer.py
@apps/backend/core/file_utils.py
</execution_context>

<context>
**저장 위치**: `.auto-claude/leader_context/` 또는 `.planning/leader_context/`
**파일 형식**: JSON
**파일 이름**: `context_{phase}.json` (phase별 분리) 또는 `context.json` (통합)

**요구사항**:
- 프로젝트별 컨텍스트 저장
- Phase별 컨텍스트 분리 가능
- 자동 백업 (이전 버전 유지)
- 동시 접근 안전 (락킹)
</context>

<tasks>

<task type="implement">
<name>ContextStorage 클래스 구현</name>
<files>apps/backend/extensions/ceo/leader_context/storage.py</files>
<action>
파일 시스템 기반 스토리지 구현:

```python
class ContextStorage:
    def __init__(self, project_path: str, storage_dir: str = ".planning/leader_context"):
        self.project_path = project_path
        self.storage_path = Path(project_path) / storage_dir

    def save(self, context: LeaderContext) -> bool:
        """컨텍스트 저장 (자동 백업 포함)"""
        # 1. 기존 파일 백업 (.bak)
        # 2. JSON 직렬화
        # 3. 파일 쓰기
        # 4. updated_at 갱신

    def load(self, phase: Optional[int] = None) -> Optional[LeaderContext]:
        """컨텍스트 로드"""
        # phase 지정 시 해당 phase만
        # 없으면 최신 컨텍스트

    def list_phases(self) -> List[int]:
        """저장된 phase 목록 반환"""

    def delete(self, phase: int) -> bool:
        """특정 phase 컨텍스트 삭제"""

    def backup(self, context: LeaderContext) -> str:
        """수동 백업, 백업 파일 경로 반환"""

    def restore(self, backup_path: str) -> Optional[LeaderContext]:
        """백업에서 복원"""
```
</action>
<verify>저장/로드 왕복 테스트, 백업 생성 확인</verify>
<done>save, load, list_phases, backup, restore 메서드 모두 구현됨</done>
</task>

<task type="implement">
<name>컨텍스트 병합 로직 구현</name>
<files>apps/backend/extensions/ceo/leader_context/merger.py</files>
<action>
여러 컨텍스트를 병합하는 유틸리티:

```python
class ContextMerger:
    @staticmethod
    def merge(contexts: List[LeaderContext]) -> LeaderContext:
        """여러 컨텍스트를 하나로 병합"""
        # goals: 중복 제거, 최신 상태 유지
        # decisions: 시간순 정렬
        # patterns: frequency 합산
        # mistakes: 모두 유지
        # file_map: 최신 정보 우선
        # dependencies: 합집합
        # risks: 모두 유지, 중복 제거

    @staticmethod
    def diff(old: LeaderContext, new: LeaderContext) -> dict:
        """두 컨텍스트 차이 계산"""
        # 추가/수정/삭제된 항목 반환
```
</action>
<verify>병합 결과 검증, 중복 제거 확인</verify>
<done>merge, diff 메서드 구현 완료</done>
</task>

<task type="implement">
<name>컨텍스트 초기화 유틸리티</name>
<files>apps/backend/extensions/ceo/leader_context/initializer.py</files>
<action>
새 프로젝트/phase용 컨텍스트 초기화:

```python
class ContextInitializer:
    @staticmethod
    def create_empty(project_id: str, phase: int) -> LeaderContext:
        """빈 컨텍스트 생성"""

    @staticmethod
    def from_project_md(project_md_path: str) -> LeaderContext:
        """PROJECT.md에서 초기 컨텍스트 추출"""
        # constraints, domain 정보 파싱

    @staticmethod
    def from_state_md(state_md_path: str) -> LeaderContext:
        """STATE.md에서 컨텍스트 업데이트"""
        # decisions, progress 정보 파싱

    @staticmethod
    def inherit_from_phase(storage: ContextStorage, previous_phase: int) -> LeaderContext:
        """이전 phase 컨텍스트 상속"""
        # 완료되지 않은 goals 유지
        # decisions, patterns, dependencies 상속
        # phase 번호 업데이트
```
</action>
<verify>각 초기화 방법 테스트</verify>
<done>create_empty, from_project_md, from_state_md, inherit_from_phase 구현됨</done>
</task>

</tasks>

<verification>
```bash
# 스토리지 테스트
python -c "
from extensions.ceo.leader_context.storage import ContextStorage
from extensions.ceo.leader_context.models import LeaderContext
storage = ContextStorage('/tmp/test_project')
ctx = LeaderContext(project_id='test', phase=1)
storage.save(ctx)
loaded = storage.load(phase=1)
print('Storage OK:', loaded is not None)
"
```
</verification>

<success_criteria>
- [ ] storage.py에 ContextStorage 클래스 구현
- [ ] merger.py에 ContextMerger 클래스 구현
- [ ] initializer.py에 ContextInitializer 클래스 구현
- [ ] 저장/로드 왕복 테스트 통과
- [ ] 백업 파일 자동 생성 확인
</success_criteria>

<output>
- `apps/backend/extensions/ceo/leader_context/storage.py`
- `apps/backend/extensions/ceo/leader_context/merger.py`
- `apps/backend/extensions/ceo/leader_context/initializer.py`
</output>
