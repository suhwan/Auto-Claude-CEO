---
phase: 04-ceo-router
plan: 02
type: execute
depends_on: ["04-01"]
files_modified: [core/ceo_router/bridge.py, core/ceo_router/executor.py, core/ceo_router/__init__.py]
---

<objective>
Auto-Claude Bridge 구현: GSD PLAN.md를 Auto-Claude로 연결

Purpose: GSDConverter 출력(implementation_plan.json)을 Auto-Claude 파이프라인에 전달하고 결과 동기화
Output: core/ceo_router/bridge.py, core/ceo_router/executor.py
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# 선행 Plan 요약:
@.planning/phases/04-ceo-router/04-01-SUMMARY.md

# GSD Converter 컨텍스트:
@.planning/phases/03-gsd-converter/03-03-SUMMARY.md
@core/gsd_converter/converter.py
@core/gsd_converter/schemas.py

# 아키텍처:
@.planning/phases/02-architecture/integration-architecture.md

**Tech stack available:** Python 3.10+, dataclasses, pathlib, json
**Established patterns:** GSDConverter API, RoutingResult/ExecutionResult 모델
**Constraining decisions:**
- Phase 2: Auto-Claude는 implementation_plan.json 형식 필요
- Phase 3: GSDConverter.convert() API 사용
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-Claude Bridge 클래스 구현</name>
  <files>core/ceo_router/bridge.py</files>
  <action>
AutoClaudeBridge 클래스 구현:

1. __init__(self, gsd_converter: GSDConverter = None, output_dir: str = ".auto-claude"):
   - GSDConverter 인스턴스 주입 또는 기본 생성
   - output_dir: implementation_plan.json 저장 위치

2. prepare_plan(self, plan_path: str) -> ImplementationPlan:
   - GSDConverter.convert() 호출
   - implementation_plan.json 파일 생성
   - ImplementationPlan 객체 반환

3. get_plan_path(self, plan: ImplementationPlan) -> str:
   - spec_id 기반 JSON 파일 경로 반환
   - "{output_dir}/{spec_id}_implementation_plan.json"

4. sync_status(self, result: ExecutionResult) -> None:
   - STATE.md 업데이트 (Placeholder - 실제 파일 수정은 Phase 5에서)
   - 로그 출력으로 대체

5. get_progress(self, spec_id: str) -> dict:
   - implementation_plan.json 파일 읽기
   - subtask별 status 집계 반환

GSDConverter import 및 에러 핸들링 포함
  </action>
  <verify>python -c "from core.ceo_router.bridge import AutoClaudeBridge; b=AutoClaudeBridge(); print('OK')"</verify>
  <done>AutoClaudeBridge 인스턴스 생성 가능, prepare_plan 메서드 존재</done>
</task>

<task type="auto">
  <name>Task 2: Plan Executor 구현</name>
  <files>core/ceo_router/executor.py</files>
  <action>
PlanExecutor 클래스 구현 (Auto-Claude 실행 시뮬레이션):

1. __init__(self, bridge: AutoClaudeBridge = None):
   - AutoClaudeBridge 주입 또는 기본 생성

2. execute(self, plan: ImplementationPlan) -> ExecutionResult:
   - 실제 Auto-Claude 호출은 UI 통합 시 구현
   - 현재는 시뮬레이션:
     * plan.subtasks 순회
     * 각 subtask status를 "completed"로 변경 (시뮬레이션)
     * ExecutionResult 생성 반환

3. execute_subtask(self, subtask: Subtask) -> bool:
   - 개별 서브태스크 실행 시뮬레이션
   - 항상 True 반환 (실제 구현은 Auto-Claude API 호출)

4. _create_execution_result(self, plan: ImplementationPlan, completed: List[str], failed: List[str]) -> ExecutionResult:
   - 헬퍼 메서드: ExecutionResult 생성

Phase 5 (Leader Context)에서 실제 Auto-Claude API 연동 예정
현재는 인터페이스와 데이터 흐름만 구현
  </action>
  <verify>python -c "from core.ceo_router.executor import PlanExecutor; e=PlanExecutor(); print('OK')"</verify>
  <done>PlanExecutor.execute() 메서드가 ExecutionResult 반환</done>
</task>

<task type="auto">
  <name>Task 3: 통합 테스트 및 패키지 업데이트</name>
  <files>core/ceo_router/__init__.py</files>
  <action>
1. __init__.py 업데이트:
   - AutoClaudeBridge, PlanExecutor export 추가
   - __all__ 리스트에 추가

2. 통합 흐름 검증:
   ```python
   from core.gsd_converter import GSDConverter
   from core.ceo_router import CEORouter, AutoClaudeBridge, PlanExecutor

   # 라우팅 → 변환 → 실행 파이프라인 테스트
   router = CEORouter()
   result = router.route("API 만들어줘")

   if result.target == "auto-claude":
       bridge = AutoClaudeBridge()
       plan = bridge.prepare_plan("path/to/PLAN.md")
       executor = PlanExecutor(bridge)
       exec_result = executor.execute(plan)
   ```

3. Import 순환 의존성 없음 확인
  </action>
  <verify>python -c "from core.ceo_router import CEORouter, AutoClaudeBridge, PlanExecutor; print('All imports OK')"</verify>
  <done>전체 파이프라인 import 성공, 순환 의존성 없음</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `from core.ceo_router import AutoClaudeBridge, PlanExecutor` 성공
- [ ] AutoClaudeBridge.prepare_plan() 메서드 존재
- [ ] PlanExecutor.execute() 메서드가 ExecutionResult 반환
- [ ] GSDConverter와 연동 동작
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No import errors or circular dependencies
- AutoClaudeBridge가 GSDConverter와 연동
- PlanExecutor가 ExecutionResult 반환
</success_criteria>

<output>
After completion, create `.planning/phases/04-ceo-router/04-02-SUMMARY.md`:

# Phase 04-02: Auto-Claude Bridge Summary

**[핵심 요약]**

## Accomplishments

- [구현된 기능]

## Files Created/Modified

- `path/to/file.py` - Description

## Decisions Made

[Key decisions and rationale]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

04-03과 병렬 완료 후 Phase 4 완료
</output>
