---
phase: 11
plan: 1
name: New Project 마법사 UI
estimated_minutes: 25
parallel_safe: true
---

<objective>
GSD 탭에 "New Project" 버튼과 마법사 UI 추가. 프로젝트 이름, 설명, 목표를 입력받아 `.planning/` 디렉토리 구조를 자동 생성한다.
</objective>

<execution_context>
@apps/frontend/src/renderer/components/GsdView.tsx
@apps/frontend/src/main/gsd-service.ts
@apps/frontend/src/main/ipc-handlers/gsd-handlers.ts
</execution_context>

<context>
**현재 상태**:
- GSD 탭에 Timeline/Kanban/Dashboard 뷰어만 있음
- 프로젝트 생성 기능 없음
- CLI 명령어 `/gsd:new-project`로만 생성 가능

**추가할 기능**:
1. "New Project" 버튼 (ROADMAP 없을 때 표시)
2. NewProjectWizard 다이얼로그
3. GsdService.createProject() 메서드
4. `.planning/` 디렉토리 구조 생성

**생성할 구조**:
```
.planning/
├── PROJECT.md      # 프로젝트 정보
├── STATE.md        # 현재 상태
├── ROADMAP.md      # 로드맵 (빈 템플릿)
├── config.json     # GSD 설정
└── phases/         # Phase 디렉토리
```

**UI 구성**:
```
┌─────────────────────────────────────────────────────────────┐
│  GSD Tab - No Roadmap Found                                 │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  📁 No project initialized                          │   │
│  │                                                     │   │
│  │  Create a new GSD project to start planning.       │   │
│  │                                                     │   │
│  │  [+ New Project]                                    │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  New Project Wizard                               [X]       │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  Project Name:                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ My Awesome Project                                  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  Description:                                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ A brief description of what this project does...   │   │
│  │                                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  Core Value (optional):                                     │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ The main principle guiding this project            │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│                              [Cancel]  [Create Project]     │
└─────────────────────────────────────────────────────────────┘
```
</context>

<tasks>

<task type="implement">
<name>GsdService에 createProject 메서드 추가</name>
<files>apps/frontend/src/main/gsd-service.ts</files>
<action>
프로젝트 생성 메서드 추가:

```typescript
export interface CreateProjectInput {
  name: string;
  description: string;
  coreValue?: string;
}

export interface CreateProjectResult {
  success: boolean;
  projectPath: string;
  filesCreated: string[];
  error?: string;
}

async createProject(input: CreateProjectInput): Promise<CreateProjectResult> {
  const planningDir = path.join(this.projectPath, '.planning');
  const phasesDir = path.join(planningDir, 'phases');
  const filesCreated: string[] = [];

  try {
    // Create directories
    if (!fs.existsSync(planningDir)) {
      fs.mkdirSync(planningDir, { recursive: true });
    }
    if (!fs.existsSync(phasesDir)) {
      fs.mkdirSync(phasesDir, { recursive: true });
    }

    // Create PROJECT.md
    const projectMd = `# ${input.name}

## Overview

${input.description}

${input.coreValue ? `## Core Value\n\n${input.coreValue}\n` : ''}
## Key Decisions

| Decision | Rationale | Date |
|----------|-----------|------|
| Project initialized | Starting new GSD project | ${new Date().toISOString().split('T')[0]} |

## Created

- Date: ${new Date().toISOString().split('T')[0]}
- Tool: Auto-Claude GSD
`;
    fs.writeFileSync(path.join(planningDir, 'PROJECT.md'), projectMd);
    filesCreated.push('PROJECT.md');

    // Create STATE.md
    const stateMd = `# Project State

## Project Reference

See: .planning/PROJECT.md

**Core value:** ${input.coreValue || 'Not defined'}
**Current focus:** Initial Setup

## Current Position

Phase: 0 of 0
Plan: 0/0 completed
Status: Awaiting roadmap creation
Last activity: ${new Date().toISOString().split('T')[0]} — Project initialized

Progress: ░░░░░░░░░░ 0%

## Next Steps

1. Create roadmap: Use "Create Roadmap" button
2. Plan first phase
3. Execute plans
`;
    fs.writeFileSync(path.join(planningDir, 'STATE.md'), stateMd);
    filesCreated.push('STATE.md');

    // Create empty ROADMAP.md template
    const roadmapMd = `# Roadmap: ${input.name}

## Overview

${input.description}

## Phases

No phases defined yet. Use "Create Roadmap" to generate phases.

## Progress

| Phase | Plans Complete | Status | Completed |
|-------|----------------|--------|-----------|
| - | - | - | - |
`;
    fs.writeFileSync(path.join(planningDir, 'ROADMAP.md'), roadmapMd);
    filesCreated.push('ROADMAP.md');

    // Create config.json
    const config = {
      mode: 'standard',
      depth: 'moderate',
      parallelization: {
        enabled: true,
        plan_level: true,
        task_level: false,
        max_concurrent_agents: 3
      },
      gates: {
        confirm_project: true,
        confirm_phases: true,
        confirm_plan: true
      }
    };
    fs.writeFileSync(path.join(planningDir, 'config.json'), JSON.stringify(config, null, 2));
    filesCreated.push('config.json');

    return {
      success: true,
      projectPath: planningDir,
      filesCreated
    };
  } catch (error) {
    return {
      success: false,
      projectPath: planningDir,
      filesCreated,
      error: error instanceof Error ? error.message : 'Unknown error'
    };
  }
}
```
</action>
<verify>TypeScript 컴파일 확인</verify>
<done>GsdService에 createProject 메서드 추가됨</done>
</task>

<task type="implement">
<name>IPC 핸들러 및 Preload API 추가</name>
<files>apps/frontend/src/main/ipc-handlers/gsd-handlers.ts, apps/frontend/src/preload/api/modules/gsd-api.ts, apps/frontend/src/shared/constants/ipc.ts</files>
<action>
IPC 상수 추가:
```typescript
GSD_CREATE_PROJECT: 'gsd:createProject',
```

IPC 핸들러 추가:
```typescript
ipcMain.handle(
  IPC_CHANNELS.GSD_CREATE_PROJECT,
  async (
    _event: IpcMainInvokeEvent,
    projectPath: string,
    input: { name: string; description: string; coreValue?: string }
  ): Promise<IPCResult> => {
    try {
      logger.info('gsd:createProject', { projectPath, name: input.name });
      const gsdService = getGsdService(projectPath);
      const result = await gsdService.createProject(input);
      return { success: result.success, data: result, error: result.error };
    } catch (error) {
      logger.error('gsd:createProject failed:', error);
      return { success: false, error: error instanceof Error ? error.message : 'Unknown error' };
    }
  }
);
```

Preload API 추가:
```typescript
export interface CreateProjectInput {
  name: string;
  description: string;
  coreValue?: string;
}

export interface CreateProjectResult {
  success: boolean;
  projectPath: string;
  filesCreated: string[];
  error?: string;
}

// In GsdAPI interface:
createProject: (projectPath: string, input: CreateProjectInput) => Promise<IPCResult<CreateProjectResult>>;

// In createGsdAPI:
createProject: (projectPath: string, input: CreateProjectInput): Promise<IPCResult<CreateProjectResult>> =>
  invokeIpc(IPC_CHANNELS.GSD_CREATE_PROJECT, projectPath, input),
```
</action>
<verify>IPC 호출 테스트</verify>
<done>IPC 핸들러 및 Preload API 추가됨</done>
</task>

<task type="implement">
<name>NewProjectWizard 컴포넌트 구현</name>
<files>apps/frontend/src/renderer/components/gsd/NewProjectWizard.tsx</files>
<action>
프로젝트 생성 마법사 컴포넌트:

```tsx
import { useState } from 'react';
import { useTranslation } from 'react-i18next';
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
} from '../ui/dialog';
import { Button } from '../ui/button';
import { Input } from '../ui/input';
import { Textarea } from '../ui/textarea';
import { Label } from '../ui/label';
import { Loader2, FolderPlus } from 'lucide-react';
import { Alert, AlertDescription } from '../ui/alert';

interface NewProjectWizardProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  projectPath: string;
  onProjectCreated: () => void;
}

export function NewProjectWizard({
  open,
  onOpenChange,
  projectPath,
  onProjectCreated
}: NewProjectWizardProps) {
  const { t } = useTranslation(['navigation', 'common']);
  const [name, setName] = useState('');
  const [description, setDescription] = useState('');
  const [coreValue, setCoreValue] = useState('');
  const [creating, setCreating] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const handleCreate = async () => {
    if (!name.trim()) {
      setError(t('navigation:gsd.projectNameRequired'));
      return;
    }
    if (!description.trim()) {
      setError(t('navigation:gsd.projectDescriptionRequired'));
      return;
    }

    setCreating(true);
    setError(null);

    try {
      const result = await window.electronAPI.gsd.createProject(projectPath, {
        name: name.trim(),
        description: description.trim(),
        coreValue: coreValue.trim() || undefined
      });

      if (result.success) {
        onProjectCreated();
        onOpenChange(false);
        // Reset form
        setName('');
        setDescription('');
        setCoreValue('');
      } else {
        setError(result.error || t('navigation:gsd.createProjectFailed'));
      }
    } catch (err) {
      setError(err instanceof Error ? err.message : t('navigation:gsd.createProjectFailed'));
    } finally {
      setCreating(false);
    }
  };

  return (
    <Dialog open={open} onOpenChange={onOpenChange}>
      <DialogContent className="sm:max-w-[500px]">
        <DialogHeader>
          <DialogTitle className="flex items-center gap-2">
            <FolderPlus className="h-5 w-5" />
            {t('navigation:gsd.newProject')}
          </DialogTitle>
          <DialogDescription>
            {t('navigation:gsd.newProjectDescription')}
          </DialogDescription>
        </DialogHeader>

        <div className="space-y-4 py-4">
          <div className="space-y-2">
            <Label htmlFor="project-name">{t('navigation:gsd.projectName')} *</Label>
            <Input
              id="project-name"
              value={name}
              onChange={(e) => setName(e.target.value)}
              placeholder={t('navigation:gsd.projectNamePlaceholder')}
            />
          </div>

          <div className="space-y-2">
            <Label htmlFor="project-description">{t('navigation:gsd.projectDescription')} *</Label>
            <Textarea
              id="project-description"
              value={description}
              onChange={(e) => setDescription(e.target.value)}
              placeholder={t('navigation:gsd.projectDescriptionPlaceholder')}
              rows={3}
            />
          </div>

          <div className="space-y-2">
            <Label htmlFor="core-value">{t('navigation:gsd.coreValue')}</Label>
            <Input
              id="core-value"
              value={coreValue}
              onChange={(e) => setCoreValue(e.target.value)}
              placeholder={t('navigation:gsd.coreValuePlaceholder')}
            />
            <p className="text-xs text-muted-foreground">
              {t('navigation:gsd.coreValueHint')}
            </p>
          </div>

          {error && (
            <Alert variant="destructive">
              <AlertDescription>{error}</AlertDescription>
            </Alert>
          )}
        </div>

        <DialogFooter>
          <Button variant="outline" onClick={() => onOpenChange(false)} disabled={creating}>
            {t('common:buttons.cancel')}
          </Button>
          <Button onClick={handleCreate} disabled={creating}>
            {creating ? (
              <>
                <Loader2 className="h-4 w-4 mr-2 animate-spin" />
                {t('common:labels.creating')}
              </>
            ) : (
              <>
                <FolderPlus className="h-4 w-4 mr-2" />
                {t('navigation:gsd.createProject')}
              </>
            )}
          </Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  );
}
```
</action>
<verify>NewProjectWizard 렌더링 확인</verify>
<done>NewProjectWizard 컴포넌트 구현됨</done>
</task>

<task type="implement">
<name>GsdView에 New Project 버튼 추가</name>
<files>apps/frontend/src/renderer/components/GsdView.tsx</files>
<action>
GsdView에 New Project 기능 통합:

```tsx
import { NewProjectWizard } from './gsd/NewProjectWizard';

// Add state
const [showNewProjectWizard, setShowNewProjectWizard] = useState(false);

// Update the "No roadmap found" section:
if (!roadmap || roadmap.phases.length === 0) {
  return (
    <div className="h-full flex flex-col items-center justify-center p-8">
      <FolderOpen className="h-16 w-16 text-muted-foreground/50 mb-4" />
      <h3 className="text-lg font-medium text-muted-foreground mb-2">
        {t('navigation:gsd.noRoadmap')}
      </h3>
      <p className="text-sm text-muted-foreground text-center max-w-md mb-4">
        {t('navigation:gsd.noRoadmapDescription')}
      </p>
      <div className="flex gap-2">
        <Button onClick={() => setShowNewProjectWizard(true)}>
          <FolderPlus className="h-4 w-4 mr-2" />
          {t('navigation:gsd.newProject')}
        </Button>
        <Button variant="outline" onClick={handleRefresh}>
          <RefreshCw className="h-4 w-4 mr-2" />
          {t('common:buttons.refresh')}
        </Button>
      </div>

      <NewProjectWizard
        open={showNewProjectWizard}
        onOpenChange={setShowNewProjectWizard}
        projectPath={projectPath}
        onProjectCreated={loadGsdData}
      />
    </div>
  );
}

// Also add the wizard to the main render (for creating new project even when one exists):
// At the end of the component, before final closing tag
<NewProjectWizard
  open={showNewProjectWizard}
  onOpenChange={setShowNewProjectWizard}
  projectPath={projectPath}
  onProjectCreated={loadGsdData}
/>
```
</action>
<verify>New Project 버튼 동작 확인</verify>
<done>GsdView에 New Project 버튼 추가됨</done>
</task>

<task type="implement">
<name>gsd 컴포넌트 export 및 i18n 업데이트</name>
<files>apps/frontend/src/renderer/components/gsd/index.ts, apps/frontend/src/shared/i18n/locales/en/navigation.json, apps/frontend/src/shared/i18n/locales/fr/navigation.json</files>
<action>
gsd/index.ts에 export 추가:
```typescript
export { NewProjectWizard } from './NewProjectWizard';
```

i18n 번역 키 추가 (en/navigation.json):
```json
"newProject": "New Project",
"newProjectDescription": "Create a new GSD project with planning structure",
"projectName": "Project Name",
"projectNamePlaceholder": "My Awesome Project",
"projectNameRequired": "Project name is required",
"projectDescription": "Description",
"projectDescriptionPlaceholder": "A brief description of what this project does...",
"projectDescriptionRequired": "Description is required",
"coreValue": "Core Value (optional)",
"coreValuePlaceholder": "The main principle guiding this project",
"coreValueHint": "A guiding principle for all decisions in this project",
"createProject": "Create Project",
"createProjectFailed": "Failed to create project"
```

i18n 번역 키 추가 (fr/navigation.json):
```json
"newProject": "Nouveau Projet",
"newProjectDescription": "Créer un nouveau projet GSD avec structure de planification",
"projectName": "Nom du Projet",
"projectNamePlaceholder": "Mon Super Projet",
"projectNameRequired": "Le nom du projet est requis",
"projectDescription": "Description",
"projectDescriptionPlaceholder": "Une brève description de ce que fait ce projet...",
"projectDescriptionRequired": "La description est requise",
"coreValue": "Valeur Fondamentale (optionnel)",
"coreValuePlaceholder": "Le principe directeur de ce projet",
"coreValueHint": "Un principe directeur pour toutes les décisions de ce projet",
"createProject": "Créer le Projet",
"createProjectFailed": "Échec de la création du projet"
```
</action>
<verify>번역 키 로드 확인</verify>
<done>gsd 컴포넌트 export 및 i18n 업데이트됨</done>
</task>

</tasks>

<verification>
```bash
# TypeScript 컴파일 확인
cd apps/frontend && npm run typecheck

# 개발 서버 실행
npm run dev

# GSD 탭에서 확인:
# - ROADMAP 없는 프로젝트에서 "New Project" 버튼 표시
# - 버튼 클릭 시 마법사 다이얼로그 열림
# - 이름, 설명 입력 후 "Create Project" 클릭
# - .planning/ 디렉토리 생성 확인
# - PROJECT.md, STATE.md, ROADMAP.md, config.json 생성 확인
```
</verification>

<success_criteria>
- [ ] GsdService에 createProject 메서드 추가
- [ ] IPC 핸들러 GSD_CREATE_PROJECT 등록
- [ ] NewProjectWizard 컴포넌트 구현
- [ ] GsdView에 New Project 버튼 추가
- [ ] .planning/ 디렉토리 구조 생성
- [ ] PROJECT.md, STATE.md, ROADMAP.md 템플릿 생성
- [ ] i18n 번역 키 추가
</success_criteria>

<output>
- `apps/frontend/src/main/gsd-service.ts` (수정)
- `apps/frontend/src/main/ipc-handlers/gsd-handlers.ts` (수정)
- `apps/frontend/src/preload/api/modules/gsd-api.ts` (수정)
- `apps/frontend/src/shared/constants/ipc.ts` (수정)
- `apps/frontend/src/renderer/components/gsd/NewProjectWizard.tsx` (신규)
- `apps/frontend/src/renderer/components/gsd/index.ts` (수정)
- `apps/frontend/src/renderer/components/GsdView.tsx` (수정)
- `apps/frontend/src/shared/i18n/locales/en/navigation.json` (수정)
- `apps/frontend/src/shared/i18n/locales/fr/navigation.json` (수정)
</output>
