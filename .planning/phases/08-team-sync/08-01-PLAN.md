---
phase: 8
plan: 1
name: Sync Points 구현
estimated_minutes: 15
parallel_safe: true
---

<objective>
팀 간 동기화 포인트(Sync Points) 시스템 구현. 특정 마일스톤이나 의존성 지점에서 팀 간 상태를 동기화하고 진행을 조율하는 메커니즘.
</objective>

<execution_context>
@apps/backend/extensions/ceo/team_contracts/models.py
@apps/backend/extensions/ceo/team_contracts/service.py
@apps/backend/extensions/ceo/leader_context/service.py
@apps/backend/extensions/ceo/leader_checkpoints/service.py
</execution_context>

<context>
**Sync Point 개념**:
팀 간 작업 흐름에서 명시적으로 동기화가 필요한 지점.
- 의존성 완료 대기
- 병렬 작업 합류점
- 검토/승인 게이트
- 통합 테스트 지점

**저장 위치**: `.planning/team_sync/sync_points/`
**파일 형식**: JSON

**동기화 유형**:
- Barrier: 모든 팀이 도착해야 진행
- Wait: 특정 팀의 완료를 대기
- Signal: 다른 팀에게 알림만 발송
</context>

<tasks>

<task type="implement">
<name>SyncPoint 데이터 모델 정의</name>
<files>apps/backend/extensions/ceo/team_sync/models.py</files>
<action>
Sync Point 관련 데이터 모델:

```python
from enum import Enum
from dataclasses import dataclass, field
from datetime import datetime
from typing import Optional, List, Dict, Any

class SyncType(Enum):
    """동기화 유형"""
    BARRIER = "barrier"        # 모든 참여자 대기
    WAIT = "wait"              # 특정 팀 대기
    SIGNAL = "signal"          # 알림만 발송
    GATE = "gate"              # 승인 필요

class SyncStatus(Enum):
    """동기화 상태"""
    PENDING = "pending"        # 대기 중
    WAITING = "waiting"        # 일부 도착, 대기 중
    READY = "ready"            # 모든 조건 충족
    COMPLETED = "completed"    # 완료
    TIMEOUT = "timeout"        # 시간 초과
    CANCELLED = "cancelled"    # 취소됨

@dataclass
class SyncParticipant:
    """동기화 참여자"""
    team_id: str
    team_name: str
    role: str  # 'required', 'optional', 'observer'
    arrived: bool = False
    arrived_at: Optional[datetime] = None
    data: Dict[str, Any] = field(default_factory=dict)  # 참여자가 가져온 데이터

@dataclass
class SyncCondition:
    """동기화 조건"""
    condition_type: str  # 'all_arrived', 'min_count', 'specific_teams', 'timeout'
    value: Any = None    # 조건 값 (예: min_count=3, teams=['team-a', 'team-b'])
    met: bool = False
    met_at: Optional[datetime] = None

@dataclass
class SyncPoint:
    """동기화 포인트"""
    id: str
    name: str
    sync_type: SyncType
    status: SyncStatus = SyncStatus.PENDING

    # 참여자
    participants: List[SyncParticipant] = field(default_factory=list)
    required_count: int = 0  # 필수 참여자 수 (0 = 모두)

    # 조건
    conditions: List[SyncCondition] = field(default_factory=list)

    # 컨텍스트
    phase: int = 0
    description: str = ""
    context: Dict[str, Any] = field(default_factory=dict)

    # 타임스탬프
    created_at: datetime = field(default_factory=datetime.now)
    timeout_at: Optional[datetime] = None
    completed_at: Optional[datetime] = None

    # 결과
    result: Dict[str, Any] = field(default_factory=dict)
    # 모든 참여자 데이터 병합 결과

    # 콜백
    on_ready_callback: Optional[str] = None  # 준비 완료 시 호출할 함수명
    on_timeout_callback: Optional[str] = None

@dataclass
class SyncEvent:
    """동기화 이벤트 로그"""
    id: str
    sync_point_id: str
    event_type: str  # 'created', 'participant_arrived', 'condition_met', 'completed', 'timeout'
    timestamp: datetime
    team_id: Optional[str] = None
    details: Dict[str, Any] = field(default_factory=dict)
```
</action>
<verify>모델 import 테스트</verify>
<done>SyncType, SyncStatus, SyncPoint, SyncParticipant 등 정의됨</done>
</task>

<task type="implement">
<name>SyncPointManager 클래스 구현</name>
<files>apps/backend/extensions/ceo/team_sync/sync_manager.py</files>
<action>
Sync Point 관리자 구현:

```python
class SyncPointManager:
    """Sync Point 생성 및 관리"""

    def __init__(self, project_path: str):
        self.project_path = project_path
        self.sync_dir = Path(project_path) / ".planning" / "team_sync" / "sync_points"
        self.sync_dir.mkdir(parents=True, exist_ok=True)

        self._sync_points: Dict[str, SyncPoint] = {}
        self._events: List[SyncEvent] = []
        self._callbacks: Dict[str, Callable] = {}
        self._load()

    # 생성
    def create_barrier(
        self,
        name: str,
        teams: List[str],
        phase: int = 0,
        timeout_minutes: int = None
    ) -> SyncPoint:
        """Barrier 동기화 포인트 생성"""
        sync_point = SyncPoint(
            id=f"sync-{uuid.uuid4().hex[:8]}",
            name=name,
            sync_type=SyncType.BARRIER,
            phase=phase,
            participants=[
                SyncParticipant(team_id=t, team_name=t, role='required')
                for t in teams
            ],
            required_count=len(teams),
            conditions=[
                SyncCondition('all_arrived')
            ]
        )

        if timeout_minutes:
            sync_point.timeout_at = datetime.now() + timedelta(minutes=timeout_minutes)
            sync_point.conditions.append(SyncCondition('timeout', timeout_minutes))

        self._sync_points[sync_point.id] = sync_point
        self._log_event(sync_point.id, 'created')
        self._save()
        return sync_point

    def create_wait(
        self,
        name: str,
        wait_for_teams: List[str],
        waiting_team: str,
        phase: int = 0
    ) -> SyncPoint:
        """Wait 동기화 포인트 생성"""
        participants = [
            SyncParticipant(team_id=t, team_name=t, role='required')
            for t in wait_for_teams
        ]
        participants.append(
            SyncParticipant(team_id=waiting_team, team_name=waiting_team, role='observer')
        )

        sync_point = SyncPoint(
            id=f"sync-{uuid.uuid4().hex[:8]}",
            name=name,
            sync_type=SyncType.WAIT,
            phase=phase,
            participants=participants,
            required_count=len(wait_for_teams),
            conditions=[
                SyncCondition('specific_teams', wait_for_teams)
            ]
        )

        self._sync_points[sync_point.id] = sync_point
        self._log_event(sync_point.id, 'created')
        self._save()
        return sync_point

    def create_gate(
        self,
        name: str,
        approvers: List[str],
        min_approvals: int = 1,
        phase: int = 0
    ) -> SyncPoint:
        """Gate 동기화 포인트 생성 (승인 필요)"""
        sync_point = SyncPoint(
            id=f"sync-{uuid.uuid4().hex[:8]}",
            name=name,
            sync_type=SyncType.GATE,
            phase=phase,
            participants=[
                SyncParticipant(team_id=t, team_name=t, role='required')
                for t in approvers
            ],
            required_count=min_approvals,
            conditions=[
                SyncCondition('min_count', min_approvals)
            ]
        )

        self._sync_points[sync_point.id] = sync_point
        self._log_event(sync_point.id, 'created')
        self._save()
        return sync_point

    # 참여
    def arrive(
        self,
        sync_point_id: str,
        team_id: str,
        data: Dict[str, Any] = None
    ) -> Dict[str, Any]:
        """팀 도착 등록"""
        sync_point = self._sync_points.get(sync_point_id)
        if not sync_point:
            return {"success": False, "error": "Sync point not found"}

        # 참여자 찾기 및 도착 표시
        for participant in sync_point.participants:
            if participant.team_id == team_id:
                participant.arrived = True
                participant.arrived_at = datetime.now()
                participant.data = data or {}
                break
        else:
            return {"success": False, "error": "Team not in participants"}

        self._log_event(sync_point_id, 'participant_arrived', team_id)

        # 조건 체크
        self._check_conditions(sync_point)

        self._save()

        return {
            "success": True,
            "status": sync_point.status.value,
            "waiting_for": self._get_waiting_teams(sync_point)
        }

    def check_ready(self, sync_point_id: str) -> bool:
        """준비 완료 여부 확인"""
        sync_point = self._sync_points.get(sync_point_id)
        if not sync_point:
            return False
        return sync_point.status == SyncStatus.READY

    def complete(self, sync_point_id: str) -> Dict[str, Any]:
        """동기화 완료 처리"""
        sync_point = self._sync_points.get(sync_point_id)
        if not sync_point:
            return {"success": False, "error": "Sync point not found"}

        if sync_point.status != SyncStatus.READY:
            return {"success": False, "error": "Not ready yet"}

        # 결과 병합
        sync_point.result = self._merge_participant_data(sync_point)
        sync_point.status = SyncStatus.COMPLETED
        sync_point.completed_at = datetime.now()

        self._log_event(sync_point_id, 'completed')
        self._save()

        # 콜백 실행
        if sync_point.on_ready_callback and sync_point.on_ready_callback in self._callbacks:
            self._callbacks[sync_point.on_ready_callback](sync_point)

        return {
            "success": True,
            "result": sync_point.result
        }

    def cancel(self, sync_point_id: str, reason: str = "") -> bool:
        """동기화 취소"""
        sync_point = self._sync_points.get(sync_point_id)
        if not sync_point:
            return False

        sync_point.status = SyncStatus.CANCELLED
        sync_point.result["cancelled_reason"] = reason
        self._log_event(sync_point_id, 'cancelled', details={"reason": reason})
        self._save()
        return True

    # 조회
    def get(self, sync_point_id: str) -> Optional[SyncPoint]:
        """동기화 포인트 조회"""
        return self._sync_points.get(sync_point_id)

    def list_active(self, phase: int = None) -> List[SyncPoint]:
        """활성 동기화 포인트 목록"""
        result = [
            sp for sp in self._sync_points.values()
            if sp.status in [SyncStatus.PENDING, SyncStatus.WAITING]
        ]
        if phase is not None:
            result = [sp for sp in result if sp.phase == phase]
        return result

    def list_waiting_for_team(self, team_id: str) -> List[SyncPoint]:
        """팀이 대기 중인 동기화 포인트"""
        return [
            sp for sp in self._sync_points.values()
            if sp.status in [SyncStatus.PENDING, SyncStatus.WAITING]
            and any(p.team_id == team_id and not p.arrived for p in sp.participants)
        ]

    def get_events(self, sync_point_id: str) -> List[SyncEvent]:
        """이벤트 로그 조회"""
        return [e for e in self._events if e.sync_point_id == sync_point_id]

    # 콜백 등록
    def register_callback(self, name: str, callback: Callable) -> None:
        """콜백 함수 등록"""
        self._callbacks[name] = callback

    # 내부 메서드
    def _check_conditions(self, sync_point: SyncPoint) -> None:
        """조건 충족 여부 확인"""
        all_met = True
        for condition in sync_point.conditions:
            if condition.condition_type == 'all_arrived':
                required = [p for p in sync_point.participants if p.role == 'required']
                condition.met = all(p.arrived for p in required)
            elif condition.condition_type == 'min_count':
                arrived_count = sum(1 for p in sync_point.participants if p.arrived and p.role == 'required')
                condition.met = arrived_count >= condition.value
            elif condition.condition_type == 'specific_teams':
                condition.met = all(
                    any(p.team_id == t and p.arrived for p in sync_point.participants)
                    for t in condition.value
                )
            elif condition.condition_type == 'timeout':
                if sync_point.timeout_at and datetime.now() > sync_point.timeout_at:
                    sync_point.status = SyncStatus.TIMEOUT
                    return

            if condition.met and not condition.met_at:
                condition.met_at = datetime.now()
                self._log_event(sync_point.id, 'condition_met', details={"type": condition.condition_type})

            if not condition.met:
                all_met = False

        if all_met:
            sync_point.status = SyncStatus.READY
        elif any(p.arrived for p in sync_point.participants):
            sync_point.status = SyncStatus.WAITING

    def _get_waiting_teams(self, sync_point: SyncPoint) -> List[str]:
        """대기 중인 팀 목록"""
        return [
            p.team_id for p in sync_point.participants
            if p.role == 'required' and not p.arrived
        ]

    def _merge_participant_data(self, sync_point: SyncPoint) -> Dict[str, Any]:
        """참여자 데이터 병합"""
        merged = {}
        for participant in sync_point.participants:
            if participant.data:
                merged[participant.team_id] = participant.data
        return merged

    def _log_event(self, sync_point_id: str, event_type: str, team_id: str = None, details: Dict = None) -> None:
        """이벤트 로그 기록"""
        event = SyncEvent(
            id=f"evt-{uuid.uuid4().hex[:8]}",
            sync_point_id=sync_point_id,
            event_type=event_type,
            timestamp=datetime.now(),
            team_id=team_id,
            details=details or {}
        )
        self._events.append(event)

    def _load(self) -> None:
        """파일에서 로드"""

    def _save(self) -> None:
        """파일에 저장"""
```
</action>
<verify>Sync Point 생성, 참여, 완료 테스트</verify>
<done>SyncPointManager 클래스 구현 완료</done>
</task>

<task type="implement">
<name>SyncPointSerializer 및 __init__.py</name>
<files>apps/backend/extensions/ceo/team_sync/serializer.py, apps/backend/extensions/ceo/team_sync/__init__.py</files>
<action>
직렬화 및 모듈 초기화:

```python
# serializer.py
class SyncPointSerializer:
    """SyncPoint JSON 직렬화"""

    @staticmethod
    def participant_to_dict(p: SyncParticipant) -> dict:
        return {
            "team_id": p.team_id,
            "team_name": p.team_name,
            "role": p.role,
            "arrived": p.arrived,
            "arrived_at": p.arrived_at.isoformat() if p.arrived_at else None,
            "data": p.data
        }

    @staticmethod
    def participant_from_dict(data: dict) -> SyncParticipant:
        return SyncParticipant(
            team_id=data["team_id"],
            team_name=data["team_name"],
            role=data["role"],
            arrived=data.get("arrived", False),
            arrived_at=datetime.fromisoformat(data["arrived_at"]) if data.get("arrived_at") else None,
            data=data.get("data", {})
        )

    @staticmethod
    def sync_point_to_dict(sp: SyncPoint) -> dict:
        """SyncPoint를 dictionary로 변환"""

    @staticmethod
    def sync_point_from_dict(data: dict) -> SyncPoint:
        """dictionary에서 SyncPoint 생성"""

    @staticmethod
    def event_to_dict(e: SyncEvent) -> dict:
        """SyncEvent를 dictionary로 변환"""

    @staticmethod
    def event_from_dict(data: dict) -> SyncEvent:
        """dictionary에서 SyncEvent 생성"""

# __init__.py
"""
Team Sync Module

팀 간 동기화 시스템.
Sync Points, Shared Board, Leader Meeting 기능 제공.
"""

from .models import (
    SyncType,
    SyncStatus,
    SyncParticipant,
    SyncCondition,
    SyncPoint,
    SyncEvent
)
from .sync_manager import SyncPointManager
from .serializer import SyncPointSerializer

__all__ = [
    # Enums
    "SyncType",
    "SyncStatus",
    # Models
    "SyncParticipant",
    "SyncCondition",
    "SyncPoint",
    "SyncEvent",
    # Manager
    "SyncPointManager",
    # Serializer
    "SyncPointSerializer",
]
```
</action>
<verify>import 및 직렬화 테스트</verify>
<done>SyncPointSerializer, __init__.py 구현됨</done>
</task>

</tasks>

<verification>
```bash
# 모듈 import 테스트
python -c "
from extensions.ceo.team_sync import (
    SyncType, SyncStatus, SyncPoint, SyncPointManager
)
print('All imports successful')
"

# Sync Point 테스트
python -c "
from extensions.ceo.team_sync import SyncPointManager

manager = SyncPointManager('/tmp/test_project')
barrier = manager.create_barrier('Integration Test', ['frontend', 'backend', 'qa'], phase=1)
print(f'Created barrier: {barrier.id}')

# 팀 도착
manager.arrive(barrier.id, 'frontend', {'tests': 'passed'})
manager.arrive(barrier.id, 'backend', {'api': 'ready'})
result = manager.arrive(barrier.id, 'qa', {'approved': True})
print(f'Status: {result}')
"
```
</verification>

<success_criteria>
- [ ] team_sync 디렉토리 및 __init__.py 생성
- [ ] models.py에 SyncType, SyncStatus, SyncPoint 등 정의
- [ ] sync_manager.py에 SyncPointManager 클래스 구현
- [ ] serializer.py에 SyncPointSerializer 구현
- [ ] Sync Point 생성, 참여, 완료 동작 확인
</success_criteria>

<output>
- `apps/backend/extensions/ceo/team_sync/__init__.py`
- `apps/backend/extensions/ceo/team_sync/models.py`
- `apps/backend/extensions/ceo/team_sync/sync_manager.py`
- `apps/backend/extensions/ceo/team_sync/serializer.py`
</output>
