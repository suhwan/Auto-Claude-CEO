---
phase: 6
plan: 2
name: 에러 로그 시스템
estimated_minutes: 15
parallel_safe: true
depends_on: 06-01
---

<objective>
에러 발생 시 상세 로그 기록 및 분석 시스템 구현. 에러 패턴 감지, 반복 에러 추적, 자동 분류 기능 제공.
</objective>

<execution_context>
@apps/backend/extensions/ceo/leader_checkpoints/models.py
@apps/backend/extensions/ceo/leader_checkpoints/manager.py
@apps/backend/extensions/ceo/leader_context/models.py
</execution_context>

<context>
**06-01 의존성**: CheckpointManager, Checkpoint 모델 구현됨

**에러 로그 요구사항**:
- 에러 발생 시점, 컨텍스트, 스택 트레이스 기록
- 에러 심각도 분류 (warning, error, critical)
- 반복 에러 감지 및 그룹화
- 에러 → 해결 매핑
- 에러 패턴 분석

**저장 위치**: `.planning/leader_context/errors/`
**파일 형식**: `errors_{phase}.json` (phase별 통합 로그)
</context>

<tasks>

<task type="implement">
<name>ErrorLog 데이터 모델 정의</name>
<files>apps/backend/extensions/ceo/leader_checkpoints/error_models.py</files>
<action>
에러 로그 관련 데이터 모델:

```python
from enum import Enum
from dataclasses import dataclass, field
from datetime import datetime
from typing import Optional, List, Dict, Any

class ErrorSeverity(Enum):
    """에러 심각도"""
    WARNING = "warning"
    ERROR = "error"
    CRITICAL = "critical"

class ErrorCategory(Enum):
    """에러 카테고리"""
    SYNTAX = "syntax"
    RUNTIME = "runtime"
    LOGIC = "logic"
    INTEGRATION = "integration"
    CONFIGURATION = "configuration"
    PERMISSION = "permission"
    NETWORK = "network"
    TIMEOUT = "timeout"
    UNKNOWN = "unknown"

@dataclass
class ErrorContext:
    """에러 발생 컨텍스트"""
    file_path: Optional[str] = None
    function_name: Optional[str] = None
    line_number: Optional[int] = None
    subtask_id: Optional[str] = None
    agent_type: Optional[str] = None
    command: Optional[str] = None
    input_data: Optional[Dict[str, Any]] = None

@dataclass
class ErrorLog:
    """에러 로그 항목"""
    id: str
    phase: int
    timestamp: datetime
    severity: ErrorSeverity
    category: ErrorCategory
    message: str
    stack_trace: Optional[str] = None
    context: ErrorContext = field(default_factory=ErrorContext)

    # 해결 상태
    resolved: bool = False
    resolution: Optional[str] = None
    resolved_at: Optional[datetime] = None

    # 그룹화
    error_hash: Optional[str] = None  # 유사 에러 그룹화용
    occurrence_count: int = 1         # 동일 에러 발생 횟수
    first_seen: Optional[datetime] = None
    last_seen: Optional[datetime] = None

    # 관련 참조
    checkpoint_id: Optional[str] = None
    related_error_ids: List[str] = field(default_factory=list)

@dataclass
class ErrorPattern:
    """반복 에러 패턴"""
    id: str
    error_hash: str
    category: ErrorCategory
    message_pattern: str
    occurrence_count: int
    first_seen: datetime
    last_seen: datetime
    example_error_ids: List[str] = field(default_factory=list)
    suggested_fix: Optional[str] = None
    auto_resolved: bool = False
```
</action>
<verify>모델 import 및 생성 테스트</verify>
<done>ErrorSeverity, ErrorCategory, ErrorContext, ErrorLog, ErrorPattern 정의됨</done>
</task>

<task type="implement">
<name>ErrorLogger 클래스 구현</name>
<files>apps/backend/extensions/ceo/leader_checkpoints/error_logger.py</files>
<action>
에러 로그 관리자 구현:

```python
import hashlib

class ErrorLogger:
    """에러 로그 기록 및 관리"""

    def __init__(self, project_path: str, phase: int):
        self.project_path = project_path
        self.phase = phase
        self.errors_dir = Path(project_path) / ".planning" / "leader_context" / "errors"
        self.errors_dir.mkdir(parents=True, exist_ok=True)

        self._errors: List[ErrorLog] = []
        self._patterns: Dict[str, ErrorPattern] = {}
        self._load_errors()

    def log_error(
        self,
        message: str,
        severity: ErrorSeverity = ErrorSeverity.ERROR,
        category: ErrorCategory = ErrorCategory.UNKNOWN,
        stack_trace: str = None,
        context: ErrorContext = None,
        checkpoint_id: str = None
    ) -> ErrorLog:
        """에러 기록"""
        error_hash = self._compute_error_hash(message, category)

        # 기존 동일 에러 확인
        existing = self._find_by_hash(error_hash)
        if existing:
            existing.occurrence_count += 1
            existing.last_seen = datetime.now()
            self._save_errors()
            return existing

        error = ErrorLog(
            id=f"err-{uuid.uuid4().hex[:8]}",
            phase=self.phase,
            timestamp=datetime.now(),
            severity=severity,
            category=category,
            message=message,
            stack_trace=stack_trace,
            context=context or ErrorContext(),
            error_hash=error_hash,
            first_seen=datetime.now(),
            last_seen=datetime.now(),
            checkpoint_id=checkpoint_id
        )

        self._errors.append(error)
        self._update_patterns(error)
        self._save_errors()

        return error

    def resolve_error(self, error_id: str, resolution: str) -> bool:
        """에러 해결 기록"""
        for error in self._errors:
            if error.id == error_id:
                error.resolved = True
                error.resolution = resolution
                error.resolved_at = datetime.now()
                self._save_errors()
                return True
        return False

    def get_unresolved_errors(self) -> List[ErrorLog]:
        """미해결 에러 조회"""
        return [e for e in self._errors if not e.resolved]

    def get_critical_errors(self) -> List[ErrorLog]:
        """치명적 에러 조회"""
        return [e for e in self._errors if e.severity == ErrorSeverity.CRITICAL]

    def get_errors_by_category(self, category: ErrorCategory) -> List[ErrorLog]:
        """카테고리별 에러 조회"""
        return [e for e in self._errors if e.category == category]

    def get_frequent_errors(self, min_count: int = 2) -> List[ErrorLog]:
        """반복 에러 조회"""
        return [e for e in self._errors if e.occurrence_count >= min_count]

    def get_error_patterns(self) -> List[ErrorPattern]:
        """에러 패턴 조회"""
        return list(self._patterns.values())

    def get_error_summary(self) -> Dict[str, Any]:
        """에러 요약 정보"""
        return {
            "total_errors": len(self._errors),
            "unresolved": len(self.get_unresolved_errors()),
            "critical": len(self.get_critical_errors()),
            "by_category": {
                cat.value: len(self.get_errors_by_category(cat))
                for cat in ErrorCategory
            },
            "patterns_detected": len(self._patterns),
            "most_frequent": self._get_most_frequent_errors(5)
        }

    def suggest_fixes(self, error_id: str) -> List[str]:
        """에러 해결 제안"""
        error = self._find_by_id(error_id)
        if not error:
            return []

        suggestions = []

        # 동일 패턴의 이전 해결책 조회
        if error.error_hash in self._patterns:
            pattern = self._patterns[error.error_hash]
            if pattern.suggested_fix:
                suggestions.append(pattern.suggested_fix)

        # 유사 해결된 에러 참조
        for resolved in self._errors:
            if resolved.resolved and resolved.category == error.category:
                if resolved.resolution:
                    suggestions.append(resolved.resolution)

        return list(set(suggestions))[:5]

    def _compute_error_hash(self, message: str, category: ErrorCategory) -> str:
        """에러 해시 계산 (유사 에러 그룹화용)"""
        # 숫자, 경로 등 제거하여 패턴 추출
        normalized = re.sub(r'[0-9]+', 'N', message)
        normalized = re.sub(r'/[^\s]+', '/PATH', normalized)
        content = f"{category.value}:{normalized}"
        return hashlib.md5(content.encode()).hexdigest()[:12]

    def _update_patterns(self, error: ErrorLog) -> None:
        """에러 패턴 업데이트"""
        if error.error_hash not in self._patterns:
            self._patterns[error.error_hash] = ErrorPattern(
                id=f"pat-{error.error_hash}",
                error_hash=error.error_hash,
                category=error.category,
                message_pattern=error.message,
                occurrence_count=1,
                first_seen=error.timestamp,
                last_seen=error.timestamp,
                example_error_ids=[error.id]
            )
        else:
            pattern = self._patterns[error.error_hash]
            pattern.occurrence_count += 1
            pattern.last_seen = error.timestamp
            if len(pattern.example_error_ids) < 5:
                pattern.example_error_ids.append(error.id)

    def _load_errors(self) -> None:
        """파일에서 에러 로드"""

    def _save_errors(self) -> None:
        """에러를 파일에 저장"""

    def _find_by_id(self, error_id: str) -> Optional[ErrorLog]:
        """ID로 에러 검색"""

    def _find_by_hash(self, error_hash: str) -> Optional[ErrorLog]:
        """해시로 에러 검색"""

    def _get_most_frequent_errors(self, limit: int) -> List[Dict]:
        """가장 빈번한 에러"""
```
</action>
<verify>에러 로깅, 조회, 패턴 감지 테스트</verify>
<done>ErrorLogger 클래스 구현 완료</done>
</task>

<task type="implement">
<name>ErrorLogSerializer 구현</name>
<files>apps/backend/extensions/ceo/leader_checkpoints/error_serializer.py</files>
<action>
에러 로그 직렬화:

```python
class ErrorLogSerializer:
    """에러 로그 JSON 직렬화"""

    @staticmethod
    def error_to_dict(error: ErrorLog) -> dict:
        """ErrorLog를 dictionary로 변환"""

    @staticmethod
    def error_from_dict(data: dict) -> ErrorLog:
        """dictionary에서 ErrorLog 생성"""

    @staticmethod
    def pattern_to_dict(pattern: ErrorPattern) -> dict:
        """ErrorPattern을 dictionary로 변환"""

    @staticmethod
    def pattern_from_dict(data: dict) -> ErrorPattern:
        """dictionary에서 ErrorPattern 생성"""

    @staticmethod
    def context_to_dict(context: ErrorContext) -> dict:
        """ErrorContext를 dictionary로 변환"""

    @staticmethod
    def context_from_dict(data: dict) -> ErrorContext:
        """dictionary에서 ErrorContext 생성"""
```
</action>
<verify>직렬화/역직렬화 테스트</verify>
<done>ErrorLogSerializer 구현 완료</done>
</task>

</tasks>

<verification>
```bash
# 에러 로깅 테스트
python -c "
from extensions.ceo.leader_checkpoints.error_logger import ErrorLogger
from extensions.ceo.leader_checkpoints.error_models import ErrorSeverity, ErrorCategory

logger = ErrorLogger('/tmp/test_project', phase=1)
error = logger.log_error(
    'Connection timeout to database',
    severity=ErrorSeverity.ERROR,
    category=ErrorCategory.NETWORK
)
print(f'Error logged: {error.id}')
print(f'Summary: {logger.get_error_summary()}')
"
```
</verification>

<success_criteria>
- [ ] error_models.py에 ErrorSeverity, ErrorCategory, ErrorContext, ErrorLog, ErrorPattern 정의
- [ ] error_logger.py에 ErrorLogger 클래스 구현
- [ ] error_serializer.py에 ErrorLogSerializer 구현
- [ ] 에러 로깅, 해결, 패턴 감지 동작 확인
- [ ] __init__.py 업데이트
</success_criteria>

<output>
- `apps/backend/extensions/ceo/leader_checkpoints/error_models.py`
- `apps/backend/extensions/ceo/leader_checkpoints/error_logger.py`
- `apps/backend/extensions/ceo/leader_checkpoints/error_serializer.py`
</output>
